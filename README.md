# keirin-value-app

競輪3連単の妙味期待値を計算するアプリです。

## Ver.0.2

開催日を指定し、WINTICKETの日付別出走表から以下を自動取得します。

- 当日の競輪場
- レース番号
- 対象レースURL

次の版で、選択したレースの3連単オッズ取得を追加します。

## 更新方法

1. 更新ZIPの中身を、このリポジトリのフォルダへ上書き
2. `install.command`を実行
3. `run.command`を実行
4. GitHub Desktopで変更をCommitし、Push origin
